// Point this at your deployed backend before publishing to GitHub Pages.
// Local development (served by FastAPI itself): leave as empty string "".
// Production (GitHub Pages frontend + Render/Railway backend): set the full URL,
// e.g. "https://prospera-api.onrender.com" (no trailing slash).
window.API_BASE = "";
